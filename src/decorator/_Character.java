package decorator;

public interface _Character {
	//Choose the path for images to normal player or shielded player
	String getPath();
	void setPath(String path);
	void setArmor(int armor);
}
